[project]
name = "portal-napne"
version = "0.1.0"
description = "Portal NAPNE"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0.7",
    "django-environ>=0.14.0",
]


[tool.uv]
package = false
